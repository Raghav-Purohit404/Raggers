# Retriever module

def query_rag(query: str) -> dict:
    """
    Temporary engine stub.
    This will later contain FAISS + LLM logic.
    """
    return {
        "answer": f"[ENGINE OK] Received query: {query}",
        "confidence": 1.0
    }
